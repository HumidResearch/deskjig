<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>BentoWorktreeName</key>
	<string>$(BENTO_WORKTREE_NAME)</string>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<!-- Frozen legacy scheme: existing installs, bentoctl and user
				     automation all deliver deep links as bento://… -->
				<string>bento</string>
			</array>
		</dict>
	</array>
	<key>NSAppleEventsUsageDescription</key>
	<string>DeskJig controls supported apps to restore workspace windows.</string>
	<key>NSApplicationCrashOnExceptions</key>
	<true/>
	<key>NSScreenCaptureUsageDescription</key>
	<string>Taking preview of apps when creating workspace</string>
	<!-- Sparkle update feed.
	     The feed is a GitHub Release asset republished on every release, reached
	     through the `/releases/latest/download/` redirect so the URL is stable
	     forever (see docs/RELEASING.md for why this beats a gh-pages branch).

	     SUPublicEDKey is still a PLACEHOLDER: the EdDSA keypair is generated by
	     the maintainer with Sparkle's `generate_keys` and the PUBLIC half is
	     committed here (the private half only ever lives in the SPARKLE_PRIVATE_KEY
	     GitHub Actions secret). Until that lands:
	       - `.github/workflows/release.yml` refuses to build a release, and
	       - SparkleController treats the configuration as a placeholder and
	         never starts the updater, so no dev build shows an updater alert.
	     Replace the string below with the `SUPublicEDKey` line generate_keys
	     prints, and nothing else needs to change. -->
	<key>SUFeedURL</key>
	<string>https://github.com/armynante/deskjig/releases/latest/download/appcast.xml</string>
	<key>SUPublicEDKey</key>
	<string>REPLACE_WITH_SPARKLE_PUBLIC_KEY</string>
	<key>UTExportedTypeDeclarations</key>
	<array>
		<dict>
			<key>UTTypeConformsTo</key>
			<array>
				<string>public.data</string>
			</array>
			<key>UTTypeDescription</key>
			<string>Window Snapshot</string>
			<key>UTTypeIcons</key>
			<dict/>
			<key>UTTypeIdentifier</key>
			<string>com.nexus.windowsnapshot</string>
			<key>UTTypeTagSpecification</key>
			<dict>
				<key>public.filename-extension</key>
				<array>
					<string>windowsnapshot</string>
				</array>
			</dict>
		</dict>
		<dict/>
	</array>
</dict>
</plist>
