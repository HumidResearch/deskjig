## What / why

<!-- What does this change, and why? Link the issue (non-trivial changes should
     have one first — see CONTRIBUTING.md). -->

Closes #

## How tested

<!-- Which test plan(s) did you run, on what macOS/Xcode?
     e.g. "headless plan, macOS 15.5 / Xcode 16.3" -->

## Checklist

- [ ] `bun run build:app` succeeds
- [ ] `bun run test -- --plan headless` passes
- [ ] No drive-by renames of legacy `Bento`/`bentoctl` identifiers (tracked in dedicated issues)
- [ ] Docs updated if behavior changed
