A small polish release — and the first announced on the new [deskjig.com](https://deskjig.com).

## What's new

- The reset button next to the worktree storage folder in Quick Switch
  settings now says what it resets ("Reset worktree storage to the default
  folder") instead of a bare "Reset to default".
